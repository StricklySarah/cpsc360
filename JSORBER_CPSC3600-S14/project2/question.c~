#include "DNSClient.h"


char* makeQuestion(char* name){
	char* question = NULL;
	int index = 0;
	int length = strlen(name);

	question = (char*) malloc(sizeof(name)+16);

	//QNAME
	question[index] = 3;
	index++;
	question[index] = 
	//QTYPE use 0x0001
	question[index] = ;
	//QClass use 0x0001
	
}
